version: "2"

sql:
    - engine: "postgresql"
      schema: "../../sql/schema/001_medicines.sql"
      queries: "../../sql/queries/medicines.sql"
      gen:
        go:
            package: "medicineDb"
            out: "./medDB"
            sql_package: "pgx/v5"
            overrides:
                - db_type: "uuid"
                  go_type:
                    import: "github.com/google/uuid"
                    type: "UUID"