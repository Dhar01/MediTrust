package: authentication
output: ./auth_gen/auth.gen.go
generate:
    models: true
    # gin-server: true
    echo-server: true
    strict-server: true
    embedded-spec: true

output-options:
    skip-prune: true